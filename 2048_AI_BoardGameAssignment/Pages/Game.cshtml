@page
@model GameModel
@{
    ViewData["Title"] = "2048 Game";
    int[,] board = ViewData["Board"] as int[,];
}


@*@section scripts{
    <script>
        document.addEventListener('keydown', function (e) {
            //37 is left arrow, 39 is right arrow
            console.log(e.which);
            if(e.which === 37){
                document.getElementById("retning").value = "left"
                document.getElementById("submit").click();
            } else if (e.which === 39) {
                document.getElementById("retning").value = "right";
                //document.getElementById("board").value = @board;
            }
        });

    </script>
    <script>
        $(function () {
            $('#submit').on('click', function (evt) {
                console.log("sqoop");
                evt.preventDefault();
                $.post('', $('form').serialize(), function () {
                    console.log("sqoop");
                });
            });
        });
    </script>
}*@


<div class="board">

    @*<form asp-action="Game" asp-controller="Form">
        <input asp-for="Direction" id="retning" class="form-control" />
        <input asp-for="Board" class="form-control" id="retning" value=@board />
    </form>*@
    <button class="btn btn-primary" id="submit">Submit</button>
    <div class="row row-1">
        <div class="col tile">@board[0, 0] </div>
        <div class="col tile">@board[0, 1] </div>
        <div class="col tile">@board[0, 2] </div>
        <div class="col tile">@board[0, 3] </div>
    </div>
    <div class="row row-2">
        <div class="col tile">@board[1, 0] </div>
        <div class="col tile">@board[1, 1] </div>
        <div class="col tile">@board[1, 2] </div>
        <div class="col tile">@board[1, 3] </div>
    </div>
    <div class="row row-3">
        <div class="col tile">@board[2, 0] </div>
        <div class="col tile">@board[2, 1] </div>
        <div class="col tile">@board[2, 2] </div>
        <div class="col tile">@board[2, 3] </div>
    </div>
    <div class="row row-4">
        <div class="col tile">@board[3, 0] </div>
        <div class="col tile">@board[3, 1] </div>
        <div class="col tile">@board[3, 2] </div>
        <div class="col tile">@board[3, 3] </div>
    </div>
</div>
